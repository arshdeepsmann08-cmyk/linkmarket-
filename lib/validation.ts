import { z } from "zod"; export const credentialsSchema=z.object({name:z.string().min(2).max(80).optional(),email:z.string().email(),password:z.string().min(8).max(100)}); export const productSchema=z.object({name:z.string().min(2).max(120),description:z.string().min(10).max(1000),price:z.coerce.number().positive(),imageUrl:z.string().url(),categoryId:z.string().min(1),rating:z.coerce.number().min(0).max(5),affiliateUrl:z.string().url().refine(v=>/^https?:\/\//.test(v),"Only HTTP(S) affiliate URLs are allowed"),merchantName:z.string().min(2).max(80)}); export const commissionSchema=z.object({productId:z.string().min(1),orderReference:z.string().min(3).max(120),amount:z.coerce.number().positive(),status:z.enum(["PENDING","CONFIRMED","REJECTED","PAID"])});

import { NextRequest, NextResponse } from "next/server"; import bcrypt from "bcryptjs"; import { prisma } from "@/lib/prisma"; import { credentialsSchema } from "@/lib/validation"; import { makeSession } from "@/lib/auth";
export async function POST(req:NextRequest){const parsed=credentialsSchema.safeParse(Object.fromEntries(await req.formData()));if(!parsed.success)return NextResponse.redirect(new URL("/login?error=invalid",req.url));const user=await prisma.user.findUnique({where:{email:parsed.data.email.toLowerCase()}});if(!user||!await bcrypt.compare(parsed.data.password,user.passwordHash))return NextResponse.redirect(new URL("/login?error=invalid",req.url));const res=NextResponse.redirect(new URL("/dashboard",req.url));res.cookies.set("linkmarket_session",await makeSession({id:user.id,name:user.name,email:user.email,role:user.role as "USER"|"ADMIN"}),{httpOnly:true,sameSite:"lax",secure:process.env.NODE_ENV==="production",path:"/",maxAge:604800});return res}
